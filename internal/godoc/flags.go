package godoc

// The following flags are copied from the official go doc command.
var (
	Unexported bool // -u flag
	MatchCase  bool // -c flag
	ShowAll    bool // -all flag
	ShowCmd    bool // -cmd flag
	ShowSrc    bool // -src flag
	Short      bool // -short flag
)

var (
	NoImports  bool
	ShowStdlib bool
	NoLocation bool
)
